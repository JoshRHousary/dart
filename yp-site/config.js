/* Supabase project this site talks to. The anon key is public by design —
   row-level security in supabase/schema.sql is what protects the data. */
window.DART_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
